mutation NewScheduleMutation($station_id: Int!, $train_id: Int!, $pause: Int!, $duration: Int!) {
  createSchedule(station_id: $station_id, train_id: $train_id, pause: $pause, duration: $duration) {
    id
    pause
    duration
    station_id
    train_id
  }
}
